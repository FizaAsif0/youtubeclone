name: semesterprojectyt
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.25.4 
  firebase_auth: ^4.17.4 
  js: ^0.7.1 
  cupertino_icons: ^1.0.8
  video_player: ^2.8.3

  # 🚨 CRITICAL ADDITIONS FOR FIRESTORE AND WEB COMPATIBILITY 🚨
  cloud_firestore: ^4.14.0 
  flutter_web_plugins: 
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# 🚨 THE CRITICAL MISSING SECTION 🚨
flutter:
  # This line ensures the Material Icons font (for the search, home, etc. icons) is loaded.
  uses-material-design: true